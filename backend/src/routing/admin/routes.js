/**
 * Defines all the possible routes in the server.
 * Each route is a string containing the absolute path from the address.
 * @name routes
 * @type {json}
 */
module.exports = {
    questionRoute: '/admin/question'
};